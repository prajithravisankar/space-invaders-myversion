class Bullet {
    /**
     * 
     * @param {number} x 
     * @param {number} y 
     * @param {number} velocityY 
     * @param {number} width
     * @param {number} height
     */
    constructor(x, y, velocityY) {
        this.x = x;
        this.y = y;
        this.velocityY = velocityY;
        width = 0;
        height = 0;
    }

    /**
     * bullet moves up
     */
    moveUp() {
        // stub
    }
}